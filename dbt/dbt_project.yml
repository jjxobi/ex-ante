name: eq
version: "1.0.0"
config-version: 2
profile: eq

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  eq:
    staging:
      +materialized: view
    marts:
      +materialized: table
